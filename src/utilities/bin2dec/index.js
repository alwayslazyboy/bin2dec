/**
 * Bin2Dec
 * 
 * Converts a binary string to decimal number
 * 
 * @param {string} binaryString 
 * @returns number
 */
 export default function bin2dec (binaryString) {
    return parseInt(binaryString, 2);
}
